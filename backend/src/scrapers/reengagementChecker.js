// Re-engagement windows removed per product spec.
// Dormancy nudge (90-day no-signal flag) is a UI-only indicator — no signal generated.
export async function checkReengagementWindows() {
  return []
}
